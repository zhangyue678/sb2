#!/bin/bash

#CUDA_LAUNCH_BLOCKING=1 python train.py hparams/train_ECAPA_LSTM.yaml

count=0; while (( $count < 10 )); 
do  CUDA_LAUNCH_BLOCKING=1 python train.py hparams/train_ECAPA_LSTM.yaml;
((count=$count+1));
done;

